import { createTheme } from '@mui/material';

export const theme = createTheme({
  palette: {
    primary: {
      main: '#1f77b4',
    },
    background: {
      default: '#ffffff',
      paper: '#f0f2f6',
    },
    text: {
      primary: '#262730',
    },
},
});